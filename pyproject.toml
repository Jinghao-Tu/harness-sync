[project]
name = "harness-sync"
version = "0.1.0"
description = "Personal harness configuration and localized Codex plugin builds"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0,<7",
    "tomlkit>=0.13,<1",
]

[tool.uv]
package = false
