interface:
  display_name: "Git Commit"
  short_description: "检查 Git 改动，按需拆分为逻辑独立的原子提交，并遵循 Conventional Commits 规范"
  default_prompt: "Use $git-commit to review my Git changes and create one or more logically atomic commits using Conventional Commits, each representing one coherent change."
